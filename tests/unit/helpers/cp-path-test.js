import { cpPath } from 'wyzyr-ember/helpers/cp-path';
import { module, test } from 'qunit';

module('Unit | Helper | cp path');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = cpPath([42]);
  assert.ok(result);
});
